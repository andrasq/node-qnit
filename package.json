{
  "name": "qnit",
  "version": "0.14.5",
  "bin": { "qnit": "./bin/qnit" },
  "main": "index.js",
  "description": "simple unit test runner, similar to nodeunit and mocha (formerly node-qunit)",
  "author": "Andras",
  "license": "Apache-2.0",
  "repository": {
    "url": "git://github.com/andrasq/node-qunit",
    "type": "git"
  },
  "keywords": [
    "unit", "test", "unit test", "nodeunit", "mocha", "tdd", "Andras"
  ],
  "dependencies": {
    "aflow": "0.10.0",
    "qassert": "1.1.2",
    "qgetopt": "1.0.1",
    "qmock": "0.3.1",
    "qprintf": "0.9.2"
  },
  "scripts": {
    "coverage": "nyc --version && nyc --reporter text --reporter lcov bin/qnit test* || echo nyc not installed, cannot test coverage",
    "clean": "rm -rf .nyc_output coverage",
    "test": "bin/qnit test/test-*"
  }
}
